const addToDOM = (task) => {
return `<h1>${task.task_name}</h1>
        <h1>${task.task_date}</h1>
        <h1>${task.task_date}</h1>
        <h1>${task.UserId}</h1>

        `
}

export default addToDOM;